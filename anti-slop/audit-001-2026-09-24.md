# antislop audit 001

- **Target**: `README.md` (GitHub profile README), commit `3139d65`
- **Date**: 2026-09-24
- **Mode**: 2 (audit after the fact)
- **Direction**: no `DESIGN.md` exists yet. The owner has chosen to supply direction (see "Blocking" below).
- **Verified against**: the live profile screenshots supplied by the owner, plus source inspection. The session's network egress blocks the image hosts and `habibiahmada.dev`, so remote assets were checked by inspection and by what the screenshots prove, not by fetching.

Priority follows the rule tier: Hard Gate = HIGH, Purpose-Gate = MEDIUM, Quality Locks = LOW.

---

## HIGH (Hard Gate, absolute)

| # | Rule | Finding |
|:-:|---|---|
| 1 | R-02 | 10 em dash characters in the text: the banner `alt`, the About Me paragraph, all six project rows, and a comment. The character is forbidden outright. |
| 2 | R-25 | The header banner runs a gradient from `#0D47A1` to `#42A5F5` with white text across the whole width. Against `#42A5F5` white sits near 2.3:1, under the 4.5:1 floor. The 17px subtitle over the light end is the worst case. The footer banner repeats the same gradient reversed. |
| 3 | R-03 | The entire layout is nested HTML tables with percentage widths. On a phone, GitHub gives the 5-column project table and the two-column About / Activity rows a horizontal scrollbar, and the 44%-wide cells collapse into a column of stretched images. Mobile was never part of the design. |
| 4 | R-38 | The Recent Activity section renders as a heading with nothing under it. The `START_SECTION:activity` markers stay empty until the workflow has run on the default branch, so a section that looks final is in fact an unlabeled placeholder. |
| 5 | R-35 | Two of the three deliveries so far shipped image URLs that had never been verified, and three of them 404'd on the live page (trophy card, top-langs card, activity graph). A third delivery shipped a snake image that cannot resolve until a workflow runs. Delivering unverified assets is the failure this rule names. |
| 6 | R-26 | The `📍 Karawang, Indonesia` badge and the `🟢 Open to Work` badge are both wrapped in links to `/about`. Neither label promises that destination, so they read as decoration that happens to be clickable. |
| 7 | R-23 | The six animated tech-icon GIFs added in v2 were chosen by the agent on assumption, never confirmed, and shipped without being checked. They were removed in v3, but the same reflex produced the Octocat GIF that landed in the Achievements column. |

## MEDIUM (Purpose-Gate, technique allowed but the reason must be written)

| # | Rule | Finding |
|:-:|---|---|
| 8 | R-01 | Blue-to-lighter-blue gradient is the default treatment for the header, the footer, and most badges, with no stated purpose. The repeated rainbow divider GIF adds a second, unrelated gradient on top of it. |
| 9 | R-29 | The active palette holds more than 20 distinct colors: three blues of the house gradient, plus Intel blue, DBS blue, LinkedIn blue, YouTube red, Gmail red, Instagram pink, success green, streak orange, and the rainbow divider. The cap is 2 to 3 core colors plus 1 accent. |
| 10 | R-04 | Section icons are emoji picked for decoration, not relevance: 👋 🏆 🧰 🚀 📊 🤝, plus 🥇 🥈 🎓 in the Achievements columns. Any of them could be swapped for another without changing meaning. |
| 11 | R-19 | The same animated divider GIF repeats 7 times, and both banners carry `animation=fadeIn`. Motion is applied uniformly as decoration with no stated UX purpose. |
| 12 | Part 1 | Seven decorative arrows (`→`, `↗`), including `Live ↗` on every project row where the link text already says it is a link. |

## LOW (Quality Locks, consistency)

| # | Rule | Finding |
|:-:|---|---|
| 13 | R-20 | The keystone failure. Swap the name and photo out and this is any developer's profile README: the same stat cards, the same skillicons grid, the same streak, the same quote widget. Nothing in it is specific to Habibi. |
| 14 | R-05 | Uniform section rhythm. Every section is an emoji heading, centered content, then the identical divider. The uniformity is an accident of assembly, not a declared RHYTHM value. |
| 15 | R-31 | Not one decision has a written reason. No `DESIGN.md`, no dials, no purpose notes. Every Purpose-Gate finding above traces back to this. |
| 16 | R-16 | Buzzword copy: "Building digital experiences that actually matter", "Let's build something that actually matters" (the same phrase twice), "Tech Arsenal", and the triplet "Frontend-leaning, API-obsessed, performance-driven". |
| 17 | R-15 | The `Visit` CTA on the footer badge is generic and says nothing about what is on the other side. |
| 18 | R-11 | Every badge uses `style=for-the-badge`. Radius and weight are identical across 20 badges, so none of them can carry hierarchy. |
| 19 | R-38 | The "Random Dev Wisdom" widget prints a rotating quote by someone else, presented as a section of the owner's profile. It is real, but it is not his content and it carries no information about him. |

---

## Blocking

**Direction.** You chose to supply the direction yourself, and nothing can be rebuilt until it exists. Finding 13 is the one that matters most, and no amount of rule-fixing produces identity on its own. The questions are in the chat.

**Live data.** `www.habibiahmada.dev` is blocked by this session's network egress proxy, so the About text, project list, and achievements in the README are still the ones from your old README, unverified against your site.

## Not touched

Nothing has been modified. Per Mode 2, no fix is applied until you name the numbers you want fixed. Numbers you do not name stay as they are.
